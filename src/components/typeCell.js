const typeCell = (type, colorCode) => {
    return (
        <div className='type-cell'>
            <div>{type}</div>
            <div>**PLACEHOLDER TEXT**</div>
        </div>
    );
};

export default typeCell;
